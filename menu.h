#ifndef MENU_H
#define MENU_H

void printMenu(int, bool, char[]);
int calculateArcan(char birthDay[]);
void writeLogInFile(char str[], char fileName[] = "log.txt");
void doSomething(int, char*, bool*);
void menuFunc();

#endif MENU_H